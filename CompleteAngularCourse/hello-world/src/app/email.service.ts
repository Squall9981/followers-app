import { Injectable } from '@angular/core';

//Used when we have dependencies in the constructor
//Angular can inject dependencies in to the constructor
@Injectable()
export class EmailService {

  constructor() { }

}
